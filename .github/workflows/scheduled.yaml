name: scheduled workflow
on:
  schedule:
    - cron: '0 0 * * *' 
jobs:
  scheduled-job:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome message
        run: echo "welcome to scheduled workflow"